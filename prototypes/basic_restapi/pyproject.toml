[project]
name = "canary-api"
version = "0.1.0"
description = "Minimal restapi for running the test harness using flask and html"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.2",
    "structlog>=25.5.0",
]
